{
  "rules": {
    ".read": false,
    ".write": false,

    "usuario_aluno": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "usuario_empresa": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "publicProfiles": {
      ".read": true,
      "$slug": {
        ".write": "auth != null && (!data.exists() || data.child('uid').val() === auth.uid) && newData.child('uid').val() === auth.uid"
      }
    },

    "empresa_index": {
      "$cnpj": {
        ".read": true,
        ".write": "auth != null && (!data.exists() || data.child('uid').val() === auth.uid) && newData.child('uid').val() === auth.uid"
      }
    },

    "mensagens": {
      ".indexOn": ["remetenteId", "destinatarioId"],
      "$msgId": {
        ".read": "auth != null && (data.child('remetenteId').val() === auth.uid || data.child('destinatarioId').val() === auth.uid)",
        ".write": "auth != null && ((!data.exists() && newData.child('remetenteId').val() === auth.uid) || (data.exists() && (data.child('remetenteId').val() === auth.uid || data.child('destinatarioId').val() === auth.uid)))"
      }
    }
  }
}
